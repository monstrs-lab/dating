mutation selectProfileGender($gender: ProfileGender!) {
  selectProfileGender(input: { gender: $gender }) {
    result {
      ...ProfileFragment
    }
    errors {
      gender {
        id
        message
      }
    }
  }
}
