mutation changeProfileName($name: String!) {
  changeProfileName(input: { name: $name }) {
    result {
      ...ProfileFragment
    }
    errors {
      name {
        id
        message
      }
    }
  }
}
